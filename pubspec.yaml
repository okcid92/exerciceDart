name: task_manager
description: >-
  Gestionnaire de tâches en ligne de commande (Dart pur) illustrant
  l'héritage, les génériques, les interfaces, les exceptions personnalisées
  et la persistance JSON locale.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
